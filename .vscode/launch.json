{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "C++ Launch (interp)",
      "type": "cppdbg",
      "request": "launch",

      // 1. Mówi, jaki program uruchomić:
      "program": "${workspaceFolder}/build/interp",

      // 2. MÓWI, GDZIE GO URUCHOMIĆ (to jest rozwiązanie):
      "cwd": "${workspaceFolder}/build",

      "args": [],
      "stopAtEntry": false,
      "externalConsole": false
    }
  ]
}